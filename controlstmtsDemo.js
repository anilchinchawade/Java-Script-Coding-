// falsy valuesin JS
// false, 0, -0, BigInt 0n, "", null, undefined, NaN
// Remaining everything is true
// 

if (condition) {
    
}

if (condition) {
    
} else {
    
}

for (let index = 0; index < array.length; index++) {
    const element = array[index];
    
}

switch (key) {
    case value:
        
        break;

    default:
        break;
}